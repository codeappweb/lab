---
---
{% assign items = site.posts %}
[
{% for i in items %}
{"title": {{ i.title | jsonify }},"url": {{ i.url | relative_url | jsonify }},"desc": {{ i.description | default: '' | strip_html | strip | jsonify }},"cluster": {{ i.cluster | default: '' | jsonify }},"kind": "bai"}{% unless forloop.last %},{% endunless %}
{% endfor %}
{% assign hubs = site.pages | where: 'layout', 'hub' %}{% if hubs.size > 0 and items.size > 0 %},{% endif %}
{% for h in hubs %}
{"title": {{ h.title | jsonify }},"url": {{ h.url | relative_url | jsonify }},"desc": {{ h.description | default: '' | strip_html | strip | jsonify }},"cluster": {{ h.cluster | default: '' | jsonify }},"kind": "hub"}{% unless forloop.last %},{% endunless %}
{% endfor %}
]
